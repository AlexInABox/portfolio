name: Blogslopbot

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "blogslopbot/**"

permissions:
  contents: read

defaults:
  run:
    working-directory: blogslopbot

jobs:
  deploy:
    runs-on: ubuntu-slim